[pytest]
# So `_board.py` in this directory is importable from the target tests.
pythonpath = .
testpaths = .
addopts = -v --tb=short
markers =
    host: runs locally against fixtures (no board required)
    target: requires MPQ_SERIAL_PORT (board console over serial)
